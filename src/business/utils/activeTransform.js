const activeTransform = (active) => {
	if (active) return '1';
	else if (!a) return '0';
	else return { httpCode: 422, code: '422', message: 'active debe ser true o false' };
};

module.exports = { activeTransform };
